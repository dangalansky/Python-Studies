## -------------------- Prime Number Checker ---------------------- ##

def prime_number_check(number):
	is_prime = True
	for _ in range (2, number):
		if number % 2 == 0:
			is prime = False
	if is_prime:
		print('Prime Number!')
	else:
		print('Not a Prime Number :(')
		
